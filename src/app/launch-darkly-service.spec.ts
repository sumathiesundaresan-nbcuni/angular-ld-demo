import { LaunchDarklyService } from './launch-darkly-service';

describe('LaunchDarklyService', () => {
  it('should create an instance', () => {
    expect(new LaunchDarklyService()).toBeTruthy();
  });
});
